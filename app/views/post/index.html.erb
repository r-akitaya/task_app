<h1>一覧画面</h1>
<% @posts.each do |post| %>
   
  <article class="shoplist-item">
 
    <p class="shoplist-title"><%= post.title %></p>
    <div><%= post.body %></div>
    <span><%= link_to post.title, "/post/#{post.id}" %></span>
  </article>
<% end %>